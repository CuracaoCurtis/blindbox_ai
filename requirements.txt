# ============================================
# 盲盒AI智能导购系统 - 依赖列表
# 安装命令: pip install -r requirements.txt
# ============================================

# 核心依赖（必须）
streamlit>=1.31.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0

# 爬虫依赖（必须）
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# 可选：绕过反爬（推荐安装）
curl-cffi>=0.5.0

# AI 调用直接使用 requests，无需安装厂商 SDK
